spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/ehealth_ltrdb?useSSL=false&serverTimezone=UTC
    username: ${MARIADB_USERNAME}
    password: ${MARIADB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
openncp:
  root:
    path: /opt/openncp-configuration/
