package dk.sundhedsdatastyrelsen.ncpeh.cda.model; import lombok.Builder; import lombok.NonNull; import lombok.Value; import java.math.BigDecimal; import java.text.DecimalFormat; import java.text.DecimalFormatSymbols; import java.util.Locale; @Value @Builder(toBuilder = true) public class ActiveIngredient { private static final DecimalFormat decimalFormat = new DecimalFormat("0.##", new DecimalFormatSymbols(Locale.US)); @NonNull BigDecimal numerator; @NonNull BigDecimal denominator; @NonNull String numeratorUnit; @NonNull String denominatorUnit; @NonNull String name; String translation; public @NonNull String getNumerator() { return decimalFormat.format(numerator); } public @NonNull String getDenominator() { return decimalFormat.format(denominator); } } 