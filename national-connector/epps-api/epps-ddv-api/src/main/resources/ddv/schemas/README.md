# In case this needs to be upgraded at some point.

## DDV

 These changes have been made:

- for the types ```SSI_Modificator``` and ```SSI_DrugStrength``` simplification has
  been done on the complex types to make the generated classes easier to use

These are commented with `NCP:`.

For comparison the original file is kept here as
```MSSI_Modificator.orig.xsd```.
